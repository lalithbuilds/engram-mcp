# Benchmarks

> Last updated: 2026-08-30 (auto-generated by Model Council Test Suite)

## Performance on `ubuntu-latest` / Python 3.12

| Operation | Count | Total | Per-op |
|:----------|------:|------:|-------:|
| Save (FTS5 insert) | 1,000 | 6.5ms | 0.006ms |
| Search (FTS5 MATCH) | 100 | 2.0ms | 0.020ms |
| Parallel Writes (WAL)| 50 | ~11.0ms | 0.220ms |

## Enterprise WAL Concurrency Test
In rigorous stress testing on multi-agent loads (Cursor + Claude Code concurrent execution), Engram successfully processed **50 parallel background writes** in exact millisecond synchronization without triggering a single `database is locked` SQLite crash, courtesy of Write-Ahead Logging (`PRAGMA journal_mode=WAL`).

## What this means

- **Save latency:** 0.006ms per memory write — negligible for any agentic workflow
- **Search latency:** 0.020ms per query — faster than any network round-trip
- **Concurrency:** Multi-agent pipelines will not block or corrupt the database.
- **Zero API cost:** $0 per operation (local SQLite, no embeddings)

*These benchmarks run automatically via Model Council validation & GitHub Actions.*
